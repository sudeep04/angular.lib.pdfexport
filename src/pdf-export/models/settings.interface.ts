export interface Settings {
    // header: {
        
    // };
    // units: [{

    // }];
    captions: {
        architectureOffice: string;
        project: string;
    }
    sorting: 'assc' | 'desc';
}