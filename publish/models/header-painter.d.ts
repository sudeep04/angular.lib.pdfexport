import { PageConfig } from './page-config';
import { TableConfig } from './table-config';
export declare class HeaderPainter {
    drow(doc: any, pageConfig: PageConfig, tableConfig: TableConfig): void;
}
